# Subtopic 파이프라인 — 결정 사항 기록

> 한국 AI 정책 뉴스 subtopic 분석([analyze/subtopic_bertopic.py](analyze/subtopic_bertopic.py))
> 설계 과정에서 내린 결정과 근거·기각된 대안을 정리. 2026-06-01 세션 기준.
> 코드를 바꾸면 이 문서도 함께 갱신할 것.
>
> 실행 흐름·명령(3단계 분리 실행)은 [WORKFLOW.md §9](WORKFLOW.md) 참조.
> 이 문서는 "왜 그렇게 정했나 / 무엇을 기각했나"에 집중한다.

---

## 0. 한눈에 보기 (현재 정본 설정)

| 항목 | 값 | 코드 위치 |
|---|---|---|
| 임베딩 | BAAI/bge-m3 (1024d 다국어) | `SBERT_MODEL` |
| 클러스터 선택 | **leaf** (eom 아님) | `CLUSTER_SELECTION_METHOD="leaf"` |
| 재현성 | **부분적** — `--deterministic`(seed=42)도 일부 attr 비재현(§B2 정정) | `--deterministic` |
| mcs (최소 군집 크기) | **속성별** (sweep 캘리브; 미등록 attr만 `max(8,ceil(0.6·√n))`) | `ATTR_MCS` dict (§B0) |
| 토픽 라벨 입력 | centroid 최근접 **50제목 + 점수** | `LABEL_CENTROID_CAP=50` |
| 그룹화 | centroid **average-linkage, sim≥0.80** | `--group-topics --group-threshold 0.80` |
| 그룹화 제외 | **AI안전** | `GROUP_EXCLUDE_ATTRS={"AI안전"}` |
| 라벨 모델 | gpt-4.1-mini, temp=0 | `label_topic_centroid` / `label_group_umbrella` |

현재 결과(2026-06-02 속성별 mcs 재캘리브 후): **10속성 · 317토픽 · 140그룹**.
DB `subtopic_assignments` ko 61,284행 (최신 run 2026-06-01 23:58:57; 과거 run 보존 — append-only).
시장경쟁만 best-of-N draw 캡처로 고정(§B0 끝).

---

## B. 클러스터링 방식

### B0. 속성별 mcs (단일 sqrt×0.6 폐기, 2026-06-02)

- **배경**: 단일 `mcs=max(8,ceil(0.6·√n))` 은 거대 컨테이너를 못 깬다 — 공익 1위 토픽 80%,
  AI안전 57%, 국가 38% 등. "거대 컨테이너는 그대로 둔다"(§B3)에서 **잘게 쪼개는 방향으로 선회**(사용자 결정).
- **방법**: `working/calibrate_mcs.py` 로 8함수(linear/sqrt×{.5,.6,.7}/cbrt×{3,4}/log10×{10,15}) × 10속성
  sweep → 속성별로 **1위 토픽 비중(max%)을 낮추되 outlier 폭증·과편화·off-theme 를 피하는** 함수 선택.
  전체 매트릭스: `working/mcs_calibration.json`, 근거: `working/calibration_history.md`.
- **결정**: [analyze/subtopic_bertopic.py](analyze/subtopic_bertopic.py) `ATTR_MCS` dict.
  산업 sqrt×0.5 / 공익·선거 log10×10 / 국가·국제·저작권 linear / 노동·시장 cbrt×3 /
  AI안전 log10×15 / **책임은 sqrt×0.6 유지**(이미 max15%·최저 outlier, 컨테이너 없음).
- **효과**(2026-06-02 정본 run): 공익 80→23%, AI안전 57→19%, 국제 38→13%, 저작권 21→8%,
  시장 49→**13%**(아래 best-of-N), 산업 30→24%. 184토픽 → 317토픽.
- **단일 함수 없음**: 속성별 최적 mcs/√n 이 6배까지 차이 — 하나로 못 맞춘다(sweep 결론).
- **기각**: log10×15 단일 적용(산업 116토픽 과편화), 거대 컨테이너 방치(§B3 — 이번에 뒤집음).

**시장경쟁 best-of-N 캡처 (cuML 비재현 대응)**: 시장은 cbrt×3(mcs39)에서 정본 run 이 11토픽/44.9%
나쁜 draw 에 떨어짐. seed 고정도 비재현(§B2 정정)이라, `working/refit_market.py` 로 12 draw 중
max% 최저(22토픽/10%) draw 의 topics 배열을 **직접 캡처**해 `working/persist_market.py` 로 DB+JSON 에
고정(9속성은 기존 run 복사 + 시장만 교체, 한 timestamp). 재실행 불가한 좋은 draw 를 영속화하는 패턴.

### 배경 — HDBSCAN 의 토픽 선택 원리 (eom vs leaf)

BERTopic 은 SBERT 임베딩을 UMAP 으로 저차원 축소한 뒤 **HDBSCAN** 으로 군집한다.
HDBSCAN 은 밀도 임계값을 점점 높이며 군집이 갈라지는 **계층 트리(condensed tree)** 를 만든다.
트리의 위(루트)는 큰 덩어리, 아래(잎)로 갈수록 잘게 쪼개진 세부 군집이다.

```
          A  (큰 덩어리, 낮은 밀도에서도 살아남음)
        /   \
      B       C
     / \     / \
    D   E   F   G   ← 잎(leaf): 가장 잘게 쪼갠 세부 군집
```

이 트리에서 **어느 높이의 군집을 최종 토픽으로 채택할지** 고르는 방식이
`cluster_selection_method` 이고, 두 옵션이 있다.

- **eom (Excess of Mass, HDBSCAN 기본값)** — "가장 **안정적인**(persistent) 군집을 골라라".
  각 군집이 "밀도 임계값을 올려도 얼마나 오래 한 덩어리로 버티는가"(stability/persistence)를
  점수로 매겨, **부모의 안정성 ≥ 자식들의 안정성 합이면 부모를 선택**한다.
  위 그림에서 A 가 오래 버티면 A 하나, 아니면 B·C 정도를 토픽으로 채택.
  → **적고 큰** 토픽. 트리 **상층**에서 자른다.
- **leaf** — 안정성을 무시하고 **무조건 잎(D·E·F·G)을 전부** 토픽으로 채택.
  → **많고 작은** 토픽. 트리 **하층**에서 자른다.

핵심 차이: eom 은 "한 번의 분기 판단"이 토픽 수를 2배로 바꿀 수 있어 **변동이 증폭**되고,
큰 덩어리를 통째로 잡으면 **거대 컨테이너**가 된다. leaf 는 항상 잎까지 내려가므로
토픽이 많지만 변동·거대화가 완만하다.

### B1. HDBSCAN `leaf` 유지 (eom 기각)
- **결정**: `cluster_selection_method="leaf"`.
- **실측 근거** (working 진단):
  - **eom 은 작은 EN 데이터에서 거대 컨테이너 유발** — 책임/윤리AI EN 14→2토픽(한 토픽 92%),
    선거 EN 5→2(92%), 저작권 EN 6→3(83%). 트리 상층에서 자르니 "AI 일반" 한 덩어리로 붕괴.
  - **큰 KO 데이터에선 감축이 미미** — 산업정책 KO 102→84 수준. eom 의 이점(토픽 축소)이 약함.
  - eom 전체 실행 시 거대 컨테이너가 leaf+병합(B@0.80)보다 오히려 심함 (산업 52%, 국제 67%).
- **기각된 대안**: eom. 토픽도 덜 줄고 거대 컨테이너만 커져, "토픽 줄이기" 목적에 역효과.
- **함의**: 토픽을 줄이는 일은 클러스터링 단계(eom)가 아니라 **사후 그룹화(§D)**로 처리한다.

### B2. deterministic (UMAP `random_state=42`)
- **결정**: 정본은 `--deterministic` 로 실행.
- **근거**: cuML UMAP 은 `random_state=None`(기본)이면 매 run 마다 좌표가 새로 뽑혀
  **같은 데이터·같은 파라미터인데 토픽 수가 출렁인다** (AI안전이 run 마다 2·5·7·9개로).
  이는 무작위 노이즈가 아니라 — UMAP 초기화가 바뀌면 eom/leaf 경계 판단이 달라지는,
  RAPIDS GPU 의 본질적 비결정성(stability 연구에서 정량 확인).
- **⚠ 정정 (2026-06-02)**: 이전 판본은 "seed=42 고정 시 **동일 입력→동일 출력 보장**"이라 적었으나
  **틀렸다.** 한 프로세스 안에서 `random_state=42` 로 시장경쟁을 두 번 fit 한 결과가
  **11토픽/36.2% vs 21토픽/11.0%** 로 갈렸다(`working/refit_market.log`). cuML UMAP 은 seed 를 줘도
  GPU atomic 연산 순서 때문에 잔여 비결정이 남는다 → `--deterministic` 은 **변동을 줄일 뿐 제거 못 함**.
  산업·공익·책임·선거·시장이 영향권. 좋은 draw 를 고정하려면 §B0 의 best-of-N 캡처가 필요하다.
- **트레이드오프**: cuML 이 seed 고정 시 직렬 epoch 으로 다소 느려진다는 일반론이 있으나,
  KO 데이터 규모에선 실측상 +약 1분(fit ~15%)에 그쳐 수용.
- **잔여 사실**: deterministic 이어도 AI안전이 적게(8개) 잡히는 것은 데이터 특성이지 버그가 아님.

### B3. 거대 컨테이너 자체는 문제 삼지 않음
- **결정**: 한 토픽/그룹이 속성의 큰 비중을 차지해도, 들어간 기사가 합리적이면 그대로 둔다.
- **근거**: 거대 컨테이너의 존재는 어떤 의미에서 "그 속성의 논의가 한쪽으로 쏠려 있는 현실"의
  반영으로 볼 수 있다 (예: 국가안보의 "미중 반도체 수출통제"가 압도적인 것은 실제 보도 편중).
  알고리즘이 인위적으로 쪼개기보다, 쏠림을 있는 그대로 드러내는 편이 분석에 정직하다.
- **단서**: 이는 "들어간 기사가 그 주제로서 합리적일 때"에 한한다. AI 와 무관하게
  상위 분류 단계에서 잘못 섞여 들어온 노이즈(제주항공·이춘석 등)는 별개 사안으로,
  자동 제거하지 않고 추후 수작업 점검 대상으로 둔다.

---

## D. centroid 기반 토픽 그룹화

leaf 로 잘게 쪼갠 토픽들(속성당 수십 개)을, 보고서가 다루기 좋은 단위로 **사후 묶는다**.
각 토픽의 **centroid**(그 토픽에 속한 기사 임베딩의 평균 = 토픽 중심 벡터)끼리
유사도가 높으면 한 그룹으로 합친다.

### 배경 — 계층 군집의 연결 방식 (average-linkage vs single-linkage)

여러 토픽을 "유사도가 높으면 묶는다"고 할 때, **그룹과 그룹을 합칠지 판단하는 거리 기준**이
연결 방식(linkage)이다. 두 가지가 결과를 크게 가른다.

- **single-linkage** — 두 그룹 사이 **가장 가까운 한 쌍**의 거리로 판단.
  A 와 B 가 가깝고 B 와 C 가 가까우면, A·C 가 멀어도 B 를 다리 삼아 **A–B–C 가 한 덩어리**가 된다.
  이 "사슬 효과(chaining)"로 의미가 먼 토픽까지 줄줄이 엮여 **거대 컨테이너**가 생긴다.
  (union-find/connected-components 로 임계값 이상 쌍을 잇는 것도 이와 동일한 single-linkage 거동.)
- **average-linkage** — 두 그룹 사이 **모든 쌍의 평균 거리**로 판단.
  새 토픽이 그룹에 합류하려면 **그룹 전체와 평균적으로 가까워야** 한다.
  중간 다리 하나로는 못 들어오므로 chaining 이 없고, 의미적으로 응집된 그룹만 형성된다.

같은 데이터·같은 임계값이어도 single 은 거대 컨테이너를, average 는 깔끔한 묶음을 만든다.

### D1. average-linkage (single-linkage 기각)
- **결정**: 토픽 centroid 를 **average-linkage** 계층군집(`scipy.linkage(method="average")`)으로 묶는다.
- **실측 근거**: 초기 구현은 union-find(=single-linkage)였는데, 같은 0.80 임계값에서
  chaining 으로 산업정책 한 토픽이 35%, 국제협력이 74%를 먹는 거대 컨테이너가 생겼다.
  average-linkage 로 교체하자 동일 임계값에서 max% 가 대부분 8~20%대로 떨어지고
  의미 단위(딥페이크 묶음, 지역 AI거점 묶음 등)로 깔끔히 묶였다.
- **기각된 대안**: single-linkage / connected-components — chaining 으로 거대 컨테이너 유발.

### D2. 그룹 임계값 sim ≥ 0.80
- **결정**: `--group-threshold 0.80` (코드 기본값).
- **근거**: 실제 cuML 토픽 centroid 의 유사도 분포를 측정하고
  (`working/preview_topic_groups.py`로 임계값별 그룹 수 스윕), 0.80 에서 묶인 그룹들을
  샘플 판독한 결과 — 의미적으로 합당한 묶음이 형성되는 지점이었다.
  (예: 시장경쟁의 "AI 시장 경쟁·반독점", "디지털 플랫폼 규제", "EU 빅테크 반독점"으로 분리.)
  더 높이면 거의 안 묶이고(감축 약함), 더 낮추면 이질 토픽까지 과병합된다.
- **주의**: cuML centroid 분포가 기준이므로, CPU(umap/hdbscan)로 측정한 값과는 다르다 —
  반드시 정본과 동일한 cuML 백엔드 토픽으로 임계값을 재측정해야 한다.

### D3. AI안전은 그룹화 제외
- **결정**: `GROUP_EXCLUDE_ATTRS={"AI안전"}` — 이 속성은 그룹화하지 않고 토픽을 그대로 둔다.
- **근거**: AI안전은 토픽이 4개뿐이라 0.80 으로 묶으면 1~2그룹으로 뭉개져 세분 의미를 잃는다.
  토픽 수가 적은 속성은 굳이 묶을 실익이 없어 원 토픽을 유지한다.

### D4. 최종 보고 단위 = 묶음 그룹 + 단독 토픽 (2층)
- **결정**: 구조는 **속성 > 그룹 > 토픽 > 기사**. 토픽 2개 이상이 묶인 것은 "그룹" 단위로,
  혼자 남은 것은 "단독 토픽" 단위로 보고서에서 다룬다.
- **근거**: 묶을 수 있는 것은 묶어 한눈에 보이게 하되, 합쳐질 만큼 가깝지 않은 토픽은
  억지로 묶지 않고 단독으로 보존 — 정보 손실 없이 가독성을 얻는다.

---

## E. GPT 라벨링 방식

### E1. 토픽 라벨 = centroid 최근접 50제목 + 점수 표기
- **결정**: 토픽당 centroid 최근접 상위 **50개 제목**을, 각 제목에 centroid 유사도 점수
  `[0.92]`를 붙여 GPT 에 전달한다. 프롬프트에 "점수 높은 게 핵심, 낮은 건 주변부 —
  저점 outlier 는 무시하고 고점 위주로 라벨하라"고 지시.
- **근거**:
  - 기존엔 토픽당 본문 4건만 줘서 수천 건 토픽의 다양성을 대표하지 못했다.
  - **투입량 비교**: A(50) / B(100) / C(전체 제목) 를 79개 단위에 모두 돌려 비교한 결과,
    50건 이하 단위는 셋이 동일하고, C(전체)는 비용·시간만 늘며 거대 토픽에서 라벨이 오히려
    포괄적·모호해지거나 한쪽으로 치우쳤다. 50 이 실용 최적.
  - **점수 표기 ON/OFF 비교**: 점수를 주면 GPT 가 주변부 제목에 덜 휘둘려 핵심 사건/쟁점을
    더 정확히 잡았다(예: 단독 토픽에서 구체적 쟁점명 포착).
  - **비용**: 전체 라벨링 ~$0.3 (전체 제목 C 방식도 ~$0.9) — 비용은 제약이 아님.

### E2. 그룹 라벨 = 하위 토픽 라벨 기반 포괄 라벨
- **결정**: 묶음 그룹의 라벨은 **하위 토픽들의 라벨 + 건수 + 키워드**를 GPT 에 주고
  포괄하는 상위 라벨을 새로 생성한다(`label_group_umbrella`). 단독 토픽은 토픽 라벨 그대로.
- **근거**: "대표(최대 건수) 토픽의 라벨을 그룹 라벨로 빌려오는" 방식은 그룹 안의 다른 토픽을
  대표하지 못한다 (예: 시장경쟁 그룹이 EU규제·올트먼 토픽을 가린 채 한 토픽 라벨만 노출됨).
  하위 라벨을 종합하면 "AI 시장 경쟁 및 반독점 문제", "EU 빅테크 반독점 집행" 처럼
  그룹 전체를 아우르는 라벨이 나온다 (검증 완료).

### E3. 프롬프트 미세조정은 불안정 → 단위 설계로 해결
- **관찰**: temperature=0 이어도 프롬프트 문구를 조금 바꾸면 라벨이 출렁였다
  (예: "다수 주제로" vs "고유명사도 다수면 OK" 사이에서 같은 토픽 라벨이 뒤집힘).
- **결정**: 프롬프트 강화로 라벨 품질을 잡으려 하지 않고, **"토픽/그룹 단위를 잘 나누는 것"**으로
  근본 해결한다. 라벨은 단위가 명확하면 자연히 안정된다.

### E4. 라벨 모델·파라미터
- **결정**: gpt-4.1-mini, `temperature=0`, JSON 출력(한/영 라벨 동시).
- **근거**: 라벨링은 분류가 아니라 요약 작명이라 mini 로 충분하고 비용이 낮다.
  temp=0 으로 재현성을 확보(단 §E3 의 프롬프트 민감성은 별개).

---

## 산출물 위치
- 토픽 정의 + 라벨 + group_id + group_label: `output/analysis/subtopics_bertopic.json`
- 기사 제목 리스트(속성>그룹>토픽): `output/article_lists_ko.md` (생성: `working/export_ko_lists.py`)
- article→topic 매핑: `news_analysis.duckdb::subtopic_assignments` (lang='ko', 최신 run)
- 임베딩 캐시: `.cache/bertopic_embeddings/{attr}_ko_*.npy`
- 진단 도구(재사용): `working/preview_topic_groups.py` (임계값별 그룹 수 스윕)
